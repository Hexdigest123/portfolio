services:
  astro-app:
    build:
      context: .
      args:
        NOTION_KEY: ${NOTION_KEY}
        DATABASE_ID: ${DATABASE_ID}
        BUILD: ${BUILD}
    container_name: portfolio
    environment:
      NOTION_KEY: ${NOTION_KEY}
      DATABASE_ID: ${DATABASE_ID}
      BUILD: ${BUILD}
    ports:
      - "8080:4321"
